# Template Architecture & Storage

This directory will store master PowerPoint templates (e.g. KONE newspaper-style template) once received.

## Current Status (Phase 0)

* Master template from KONE is pending delivery.
* Detailed placeholder mapping, font embedding, and layout grid planning are deferred until template analysis.
* Do not commit large binary `.pptx` or `.potx` files directly unless approved.
